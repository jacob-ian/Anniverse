import { AnniversaryMessage } from './anniversary-message';

describe('AnniversaryMessage', () => {
  it('should create an instance', () => {
    expect(new AnniversaryMessage()).toBeTruthy();
  });
});
